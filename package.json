{
  "name": "loyalty-prime",
  "version": "1.0.0",
  "scripts": {
    "tunnel": "vk-tunnel --insecure=1 --http-protocol=http --ws-protocol=ws --host=0.0.0.0 --port=5173",
    "dev": "concurrently \"npm run dev:miniapp\" \"npm run dev:backend\" \"npm run dev:crm\"",
    "dev:miniapp": "cd mini-app && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:crm": "cd crm-panel && npm run dev",
    "install:all": "cd mini-app && npm install && cd ../backend && npm install && cd ../crm-panel && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "@vkontakte/vk-tunnel": "^0.2.10"
  }
}